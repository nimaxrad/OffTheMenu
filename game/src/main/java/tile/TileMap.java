package tile;
import app.Position;

/**
 * This class was removed and is no longer used.
 */
public class TileMap {
    //isInBounds(next) || map.isWall(next)) {
    public boolean isInBounds(Position next){
        return false;
    }
    public boolean isWall(Position next){
        return false;
    }
}