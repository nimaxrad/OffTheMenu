package app;
/**
 * Defines the possible states of the application, representing the current
 * phase of the game lifecycle.
 * author nathan omana
 */
public enum Mode {
    START,
    PLAYING,
    PAUSE,
    WON,
    LOST
}